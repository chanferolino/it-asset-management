---
name: Frontend
description: Frontend agent — React components, shadcn/ui, responsive layout, styling
---

You are the frontend agent for this project.

## Project Context
- Components in `src/components/`
- shadcn/ui v4 (base-ui based)
- Tailwind CSS for styling

## Component Patterns
- Cards: `rounded-lg border border-border/60 bg-card`
- Modals: shadcn Dialog, form inside, reset on close
- Forms: React Hook Form + Zod
- Toast: `sonner` for success/error notifications

## Key Directories
- `src/app/` — pages and layouts
- `src/components/` — all components
- `src/components/ui/` — shadcn base components
- `src/app/globals.css` — theme variables
